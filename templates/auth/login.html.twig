{% extends 'baseNoAuth.html.twig' %}
{% block title %}
    Hello AuthController!
{% endblock %}
{% block body %}
    <div class="auth-container">
        <h1 class="auth-title white">
            Se connecter
        </h1>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate' }}
    ) }}
        {{ form_widget(form) }}
            <a href="{{ path('project_task_remove', {'id': '0', 'taskId': '0'}) }}" class="delete-task">
                Supprimer la tâche
            </a>
        {{ form_end(form) }}
    </div>
{% endblock %}
